# Simplify CSV output — drop `last`, `updated`, `dte`

Remove 3 columns from the option chain CSV generated by `perform-all`.

## Task 1: Remove `last`, `updated`, `dte` from CSV output

<!-- tdd: trivial -->

Files:
- `src/model.rs`

Steps:

1. In `option_chain_to_csv_vec`, remove `"last"`, `"updated"`, `"dte"` from the header `write_record` call. The header should be:

```
"underlying", "strike", "underlying_price", "side", "bid", "mid", "ask", "bid_size", "ask_size", "expiration", "volume", "open_interest", "rate_of_return", "strike_from", "strike_to", "sharpe_ratio", "strike_percentile", "score"
```

2. In the data row `write_record` call, remove the corresponding 3 entries: `&chain.last.to_string()`, `&chain.updated`, `&chain.dte.to_string()`.

3. Run `cargo check` to confirm compilation.
